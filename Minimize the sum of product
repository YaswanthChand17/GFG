class Solution:
    def minValue(self, a, b, n):
        # Your code goes here
        a1=sorted(a)
        a2=sorted(b,reverse=True)
        s=0
        for i in range(0,n):
            s=s+(a1[i]*a2[i])
        return s
